@page
@model IndexModel
@{
    ViewData["Title"] = "Dashboard - BackOffice";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="alert alert-success">
                <h4><i class="bi bi-check-circle"></i> Sesión iniciada correctamente</h4>
                <hr>
                <p class="mb-1"><strong>Usuario:</strong> @User.Identity?.Name</p>
                <p class="mb-1"><strong>Email:</strong> @User.FindFirst(System.Security.Claims.ClaimTypes.Email)?.Value</p>
                <p class="mb-0"><strong>Tenant ID:</strong> @User.FindFirst("TenantId")?.Value</p>
            </div>
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h5><i class="bi bi-people"></i> Gestión de Usuarios</h5>
                </div>
                <div class="card-body">
                    <p>Administra los usuarios de tu organización.</p>
                    <a href="/Users/Index" class="btn btn-primary">
                        <i class="bi bi-arrow-right"></i> Ir a Usuarios
                    </a>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-info text-white">
                    <h5><i class="bi bi-info-circle"></i> Información del Sistema</h5>
                </div>
                <div class="card-body">
                    <p>Solo puedes ver y modificar datos de tu organización (Tenant ID: @User.FindFirst("TenantId")?.Value).</p>
                    <p class="text-muted mb-0">El sistema de multitenancy mantiene tus datos completamente separados.</p>
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-4">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h5><i class="bi bi-shield-check"></i> Seguridad</h5>
                </div>
                <div class="card-body">
                    <p>Todas tus acciones están protegidas por el sistema de autenticación y autorización.</p>
                    <ul>
                        <li>✅ Autenticación mediante cookies seguras</li>
                        <li>✅ Rol requerido: AdministradorBackoffice</li>
                        <li>✅ Aislamiento de datos por Tenant</li>
                        <li>✅ Redirección automática si no hay sesión iniciada</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
