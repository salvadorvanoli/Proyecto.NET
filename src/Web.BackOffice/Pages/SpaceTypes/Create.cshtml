@page
@model Web.BackOffice.Pages.SpaceTypes.CreateModel
@{
    ViewData["Title"] = "Crear Tipo de Espacio";
}

<div class="mb-4">
    <h1 class="display-4">Crear Nuevo Tipo de Espacio</h1>
</div>

@if (Model.ErrorMessage != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        @Model.ErrorMessage
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="row">
    <div class="col-md-8 col-lg-6">
        <div class="card">
            <div class="card-body">
                <form method="post">
                    <div class="mb-3">
                        <label asp-for="Input.Name" class="form-label">Nombre del Tipo de Espacio</label>
                        <input asp-for="Input.Name" class="form-control" placeholder="Ej: Sala de Reuniones, Oficina, etc." />
                        <span asp-validation-for="Input.Name" class="text-danger"></span>
                        <div class="form-text">
                            El nombre debe ser descriptivo y único dentro del tenant.
                        </div>
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-circle"></i> Crear Tipo de Espacio
                        </button>
                        <a asp-page="/SpaceTypes/Index" class="btn btn-secondary">
                            <i class="bi bi-x-circle"></i> Cancelar
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
