<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Mobile.ViewModels"
             x:Class="Mobile.Pages.CredentialPage"
             x:DataType="viewmodels:CredentialViewModel"
             Title="{Binding Title}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Header -->
            <Frame BackgroundColor="{StaticResource Primary}" Padding="20" CornerRadius="10">
                <VerticalStackLayout Spacing="10">
                    <Label Text="🪪 Credencial Digital" 
                           FontSize="24" 
                           FontAttributes="Bold"
                           HorizontalOptions="Center"
                           TextColor="White"/>
                    <Label Text="{Binding UserName}" 
                           FontSize="18"
                           HorizontalOptions="Center"
                           TextColor="White"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Configuration -->
            <Frame Padding="15" BorderColor="{StaticResource Gray200}">
                <VerticalStackLayout Spacing="15">
                    <Label Text="Configuración" FontSize="18" FontAttributes="Bold"/>
                    
                    <HorizontalStackLayout Spacing="10">
                        <Label Text="Usuario ID:" VerticalOptions="Center" WidthRequest="100"/>
                        <Entry Text="{Binding UserId}" 
                               Keyboard="Numeric" 
                               Placeholder="Ej: 1"
                               HorizontalOptions="FillAndExpand"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Spacing="10">
                        <Label Text="Credencial ID:" VerticalOptions="Center" WidthRequest="100"/>
                        <Entry Text="{Binding CredentialId}" 
                               Keyboard="Numeric" 
                               Placeholder="Ej: 1"
                               HorizontalOptions="FillAndExpand"/>
                    </HorizontalStackLayout>

                    <Button Text="💾 Guardar Credenciales" 
                            Command="{Binding SaveCredentialsCommand}"
                            BackgroundColor="{StaticResource Secondary}"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Status -->
            <Frame Padding="15" BorderColor="{StaticResource Gray200}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Estado" FontSize="18" FontAttributes="Bold"/>
                    <Label Text="{Binding StatusMessage}" 
                           FontSize="14"
                           LineBreakMode="WordWrap"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Actions -->
            <VerticalStackLayout Spacing="10">
                <Button Text="🚀 Activar Credencial" 
                        Command="{Binding StartEmulationCommand}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InvertedBoolConverter}}"
                        BackgroundColor="{StaticResource Primary}"
                        x:Name="StartButton"/>

                <Button Text="⏸️ Detener Credencial" 
                        Command="{Binding StopEmulationCommand}"
                        BackgroundColor="{StaticResource Tertiary}"
                        x:Name="StopButton"/>
            </VerticalStackLayout>

            <!-- Instructions -->
            <Frame Padding="15" BackgroundColor="{StaticResource Gray100}">
                <VerticalStackLayout Spacing="10">
                    <Label Text="📱 Instrucciones" FontSize="16" FontAttributes="Bold"/>
                    <Label FontSize="12" LineBreakMode="WordWrap">
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="1. Configura tu Usuario ID y Credencial ID&#x0a;"/>
                                <Span Text="2. Presiona 'Activar Credencial'&#x0a;"/>
                                <Span Text="3. Acerca tu celular a un punto de control&#x0a;"/>
                                <Span Text="4. El punto de control validará tu acceso&#x0a;&#x0a;"/>
                                <Span Text="⚠️ Mantén NFC activado en la configuración de tu dispositivo" FontAttributes="Bold"/>
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>
                </VerticalStackLayout>
            </Frame>

            <!-- Settings Link -->
            <Button Text="⚙️ Cambiar a Modo Punto de Control" 
                    Command="{Binding GoToSettingsCommand}"
                    BackgroundColor="{StaticResource Gray300}"
                    TextColor="Black"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
